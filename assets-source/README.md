# 원본 이미지 보관함

여기 있는 파일은 **웹사이트로 배포되지 않습니다.** (배포되는 건 `public/` 폴더뿐)
`public/assets/img/` 에 들어가는 이미지의 원본을 잃어버리지 않도록 보관하는 곳입니다.

| 원본 | 사용처 | 처리 |
|---|---|---|
| `IMG_0298.png` (1408×768, 1.05MB) | 히어로 서예 | 여백을 잘라내고 흰 여백을 순백으로 보정한 뒤 1100×416 JPEG 로 저장 → `public/assets/img/hero.jpg` (51KB) |
